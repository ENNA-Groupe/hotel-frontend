import { FindGroupPipe } from './find-group.pipe';

describe('FindGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FindGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
